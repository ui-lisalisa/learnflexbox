import React from 'react';

const Intro = () => {
  return (
    <div>
      <h2 className={'blog-title'}>Flexbox Generator</h2>
      <p className={'blog-cont'}>
        Use the key pad to generate an immediate flexable layout.
      </p>
    </div>
  );
};

export default Intro;
